let admiral: number | string = "Heddiose";

admiral;

// console.log(typeof(admiral));

// conditional checks
let scientist = Math.random() > 0.5 ? "Rosalin Franklin" : 51;

// if (scientist === "Rosalin Franklin") {
//     console.log(scientist.toUpperCase());
// } else {
//     console.error("Bukan Tipe Datanya");
// }

// if(!(typeof scientist === "string")) {
//     console.log(scientist.toFixed());
// } else {
//     console.log(scientist.toUpperCase());
// }

// The primitive
// type string represents the set of all possible strings that could ever exist; the literal
// type "Hypatia" represents just that one string.

// const adalah literal type
const pakDe: number | string = "Sagiran";

// let adalah primitive type / possible values
let ustAd : string;
ustAd = "Mahfud";
ustAd = "Leles";

// Literal Types
let lifespan : number | "ongoing" | "uncertain";

lifespan = 10;
lifespan = "ongoing";
lifespan = "uncertain";
// lifespan = "kio";

console.log(ustAd);
